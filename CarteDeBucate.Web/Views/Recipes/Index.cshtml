@model List<RecipeSummary>

@{
    ViewData["Title"] = "Retete";
}

<div class="d-flex flex-wrap align-items-center justify-content-between gap-3 mb-4">
    <h1 class="h2 mb-0">Retete</h1>
    <div class="d-flex gap-2">
        <a class="btn btn-outline-primary" asp-action="Import">Importa din URL</a>
        <a class="btn btn-primary" asp-action="Create">Adauga reteta</a>
    </div>
</div>

@if (Model.Count == 0)
{
    <div class="alert alert-info" role="status">
        Nu exista retete salvate.
    </div>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped align-middle">
            <thead>
                <tr>
                    <th scope="col">Nume</th>
                    <th scope="col">Status</th>
                    <th scope="col">Data salvarii</th>
                    <th scope="col" class="text-end">Actiuni</th>
                </tr>
            </thead>
            <tbody>
                @foreach (RecipeSummary recipe in Model)
                {
                    <tr>
                        <td>@recipe.Name</td>
                        <td>@recipe.Status</td>
                        <td>@recipe.SavedAt.ToString("dd.MM.yyyy")</td>
                        <td class="text-end text-nowrap">
                            <a class="btn btn-sm btn-outline-primary"
                               asp-action="Details"
                               asp-route-id="@recipe.Id">Detalii</a>
                            <a class="btn btn-sm btn-outline-secondary"
                               asp-action="Edit"
                               asp-route-id="@recipe.Id">Editeaza</a>
                            <a class="btn btn-sm btn-outline-danger"
                               asp-action="Delete"
                               asp-route-id="@recipe.Id">Sterge</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
